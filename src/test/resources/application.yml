jwt:
  token-issuer: funin-todo-test
  token-signing-key: funin-todo-sample-secret-key

# Test
spring:
  profiles:
    active: test
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: create-drop
  datasource:
    url: jdbc:mariadb://localhost:3306/funintodo_test?useSSL=false&useUnicode=true&characterEncoding=utf8
    username: funin
    password: aa123123
    driver-class-name: org.mariadb.jdbc.Driver