jwt:
  token-issuer: funin-todo-test
  token-signing-key: funin-todo-sample-secret-key

# Test
spring:
  profiles:
    active: test
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: create-drop